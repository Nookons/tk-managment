import {doc, getDoc, serverTimestamp, setDoc} from "firebase/firestore";
import {db} from "../../firebase";

export const AddBrokenReport = async () => {
   // Here new code to upload robot report to db
};

export const BrokenReportToHistory = async () => {
}
